{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.go) goimports -w \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Running goimports"
          }
        ]
      }
    ]
  }
}
